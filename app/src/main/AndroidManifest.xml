<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.uta.rental">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--activity android:name=".reservation.ListActivity"></activity-->
        <activity android:name=".RegisterScreen"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".AdminMainScreen"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".UserHomeScreen"
            android:theme="@style/AppTheme.NoActionBar"
            />
        <activity android:name=".DeleteReservationGUI"></activity>
        <activity android:name=".ViewAvailableCars" />
        <activity android:name=".ViewReservationDetailsGUI" />
        <activity android:name=".RegistrationScreen" />
        <activity android:name=".RentalManagerScreen" />
        <activity android:name=".reservation.ViewReservationsUserScreen"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustNothing"
            />
        <activity
            android:name=".ApplicationMainScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>